---
/**
 * Drop this once anywhere on a page to activate every [data-reveal-group]
 * marker on it (see gmt-reveal.css + lib/scroll-reveal.ts). Renders no
 * markup — just the observer wiring.
 */
---

<script>
  import { initScrollReveal } from "../lib/scroll-reveal";
  initScrollReveal();
</script>
