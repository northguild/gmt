---
title: Application Integration
description: Integrate gmt with routers, query caches, table libraries, and state management — cache-key hygiene and canonical representations at boundaries.
sidebar:

  order: 1
---

Use these patterns when gmt meets application code outside the date/time layer
itself: cache keys, URL params, table sort keys, persisted state.

## Stable cache keys

Cache keys should depend on **canonical** representations — ISO 8601 strings or
normalized epoch numbers — never on `Date` objects or fuzzy strings:

```js
// Good — canonical shapes, stable across environments
["events", { start: "2024-02-29", end: "2024-03-01" }];
["session", { at: 1710504645 }]; // epoch seconds, canonical unit
```

## Validate at boundaries

Validate timezone params before using them as a key or persisting them:

```js
import { isValidTimeZone } from "@northguild/gmt/zoned";

if (!isValidTimeZone(tzParam)) throw new Error("Invalid timezone");
```

For Unix epoch times, pick one canonical unit (milliseconds recommended) and stick to
it. Convert explicitly when needed with `convertUtcToUnix` / `convertUnixToUtc`, which
use milliseconds unless you pass `"seconds"`.

## Routers

When working with routers (Next.js, React Router, etc.), validate and canonicalize
date params before use in loaders or components. A `"2024-3-5"` param is not the
same key as `"2024-03-05"` — canonicalize with the parsing/validation layer first.

## Table libraries

For table libraries (TanStack Table, etc.), use ISO strings for sort keys to ensure
consistent ordering across environments. String comparison of ISO 8601 sorts
correctly; `Date` object comparison does not.

## The principle

Reiterate gmt's strict I/O at every boundary: public APIs accept explicit shapes
only (ISO 8601 strings, IANA timezone ids, epoch numbers). Keep canonicalized
representations at boundaries to ensure cache stability and reproducible behavior
across environments.

## See also

- [Linting](/guides/integration/linting/) — enforce these rules at the lint level
- [Core Rules](/core-rules/) — the full statement of GMT's contract
