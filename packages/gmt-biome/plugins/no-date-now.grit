`Date.now()` where {
  register_diagnostic(span=`Date.now`, message="Avoid Date.now(). Use @northguild/gmt getUnixNow({ epochUnit: 'milliseconds' | 'seconds' }) or getNow() instead.", severity="error")
}
