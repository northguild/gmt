`Date.UTC($args)` where {
  register_diagnostic(span=`Date.UTC`, message="Avoid Date.UTC(). Use @northguild/gmt convertUtcToUnix('YYYY-MM-DDTHH:mm:ssZ', { epochUnit: 'milliseconds' | 'seconds' }) instead.", severity="error")
}
